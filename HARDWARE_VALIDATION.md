# Melo 硬件写入验收

这份清单只用于受控真机验收。风扇和充电控制会写入 AppleSMC，必须在明确授权后逐步执行；日常开发与自动化测试不得安装 Helper 或发送写入命令。

## 前置条件

- 使用 `MELO_SIGN_IDENTITY=<identity> ./scripts/build-app.sh` 构建，并通过 `./scripts/verify-app.sh`。
- 主 App 与 Helper 必须具有相同 Team ID、Hardened Runtime 和预期 Bundle ID。
- 将待测 App 放在 `/Applications/Melo.app`；保留一份未安装的构建产物以便比对。
- 退出其他风扇或充电管理工具，接通电源，并保持“活动监视器”可用。
- 在 Melo 的 Doctor 中记录机型、风扇数、初始 RPM、控制模式、电池能力与 Helper 状态。
- 无内置电池的 Mac 只执行风扇部分；未报告安全上下限或写入能力的设备不得继续。

## 1. Helper 安装与身份边界

1. 在“系统状态 → 风扇与 Battery Care”点击“安装 Helper”。
2. 阅读 Melo 的能力边界说明，再请求系统安装并在 macOS 中批准后台项目。
3. 回到 Melo，确认状态为“安全 Helper 已启用”，Doctor 显示预期 Helper 协议版本。
4. 断开未签名副本或不同 Team ID 客户端的连接尝试，确认 Helper 拒绝请求。
5. 确认 Helper 只暴露结构化的状态、心跳、风扇目标/自动恢复与 Battery Care 命令；不得接受 shell 或任意路径。

## 2. 风扇受控写入

每一步都观察实际 RPM、目标 RPM 和“手动/系统自动”状态。若出现异常声响、RPM 不更新、传感器丢失或温控压力升高，立即点击“系统自动”并退出测试。

1. 记录系统自动模式下至少 10 秒的基线 RPM。
2. 选择“安静 45%”，在确认对话框中核对目标会被限制到硬件上下限后执行。
3. 确认风扇进入手动模式，实际 RPM 朝目标变化，且写后回读误差不超过 1 RPM。
4. 在 10 秒内点击“系统自动”，确认模式和 Ftst 门控均恢复。
5. 在系统温控压力仍为正常/轻度时测试；不得通过制造过热来验收。自动化策略测试必须证明 serious/critical 会立即交还 macOS。
6. 重复一次 45% 预设，然后强制退出 Melo；5 秒后确认 Helper 看门狗自动恢复系统控制。
7. 再次启动 Melo，重复一次 45% 预设，然后中断客户端连接；确认 5 秒后自动恢复。
8. 仅在前述步骤全部通过后短时测试“均衡 70%”；“全速”不是常规验收必需项。

## 3. Battery Care（仅有内置电池且能力探测支持）

1. 记录初始电量与充电状态，选择 80% 上限并确认区间为 75%–80%。
2. 在低于下限时确认允许充电；在达到上限时确认暂停充电。不得主动放电。
3. 重启 App 和 Helper，确认策略仍存在且状态可回读。
4. 点击“充到 100%”，确认临时放开充电；达到 100% 后确认自动恢复原区间。
5. 点击“停止”，确认策略文件移除且充电立即交还系统。
6. 分别验证 85% 与 90% 预设；不支持的机型必须展示“不适用”，且不能发出写入。

## 4. 恢复与通过标准

- 测试结束时所有风扇必须显示“系统自动”，Battery Care 必须处于测试计划指定的最终状态。
- 退出 Melo 后等待至少 10 秒，再用只读探针确认风扇仍为系统自动。
- 检查 Doctor 与操作记录：不得有未解释的写入失败、协议不匹配或签名拒绝。
- 通过标准：正常控制、显式恢复、断联恢复、进程退出恢复和写入失败恢复均成功；任一恢复路径失败即停止发布。
- 删除测试版 App 前，先在系统设置确认 Helper 已停用/注销；不要手工删除运行中的 LaunchDaemon 文件。
- 可在“系统状态 → 风扇与 Battery Care”点击“移除 Helper”；Melo 会先恢复系统自动模式，再断开连接并通过 ServiceManagement 注销 Helper。

## 当前真机结论

- Mac16,11（Apple M4 Pro）检测到 1 个风扇，原生只读值约 1,000 RPM。
- 该设备没有内置电池，因此 Battery Care 只能由单元测试和后续 MacBook 真机覆盖。
- Apple Development 签名链、Team ID、Hardened Runtime、嵌套 Helper、Doctor 展示和只读监测已验证。
- Helper 安装、AppleSMC 风扇写入及自动恢复仍需在获得明确授权后执行；Developer ID Application 与公证分发仍需对应证书。
